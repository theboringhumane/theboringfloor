# Available MCP servers

This Claude Code session has the MCP servers below configured.
Use only the listed server names when selecting an MCP tool.

- `thefloor_mcp` (stdio) — configured in user config

Only names, types, and provenance are listed — never commands,
arguments, URLs, headers, or environment values: this file lands in
a prompt, and prompts are no place for API keys.
