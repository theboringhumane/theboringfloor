# Video breakdown — `9dG1VVV7DG9Te-ue.mp4` (0:01–1:08.6, 1080×778, 25 fps)

## One-line summary
A product-launch promo told in **four act bars** (matching its own footer strip `Dream outcome | Solution | Demo | Outro`): *with one prompt, you dream a dream; the app builds live before your eyes; agents keep it running; ask for anything, always.*

---

## Scene-by-scene parts (frames extracted at ~6-second intervals)

### ACT 0 — "Dream outcome" (frames f001)
- **Content:** near-black card filling a white page. Clean white sans text, centered: **"with one prompt."**
- **Extra furniture:** bottom strip — a file-preview row labeled `Dream outcome | Solution | Demo | Outro`, plus a white **audio waveform** (the voiceover track, visible at every scene change).
- **Meaning:** the promise of a single sentence spanning product+office. It sets the *prompt-as-magic-wand* premise.
- **Motion vocab:** static card, typewriter-adjacent typography, hold-then-cut rhythm.

### ACT 0.5 — "Solution" hook (frame f002)
- **Content:** warm olive/brown card with typing effect: **"Build an app. With ▌"** (block cursor live).
- **Meaning:** the transformation begins — words become software. Curtain cut on a blank windowed app (no chrome) that materializes in the next beat.
- **Motion vocab:** typewriter + block-cursor blink; warm-card color shift (signal: page turn).

### ACT 1 — "Build" (frame f003)
- **Content:** a **real working UI** appears fully-formed: "Deal Tracker / Pipeline" — a CRM pipeline board (left sidebar, kanban/track rows, dollar values, colored stage chips).
- **Meaning:** the first proof — *from sentence to software in seconds*. No assembly shown; assembled state arrives whole.
- **Motion vocab:** hard pop-in of UI screenshots at full opacity; no morph, no tween.

### ACT 1.5 — bridge (frame f004, key frame ⭐)
- **Content:** back on the brown card: **"Add in agents. They keep it ru▌"**. A row of **colored pixel-block agents** — purple, yellow, green, teal — tiny figures that idle/dance across the canvas top.
- **Meaning:** THE ANCHOR SCENE. The UI alone is not the product; the tiny pixel coworkers are. From here on everything is *agents keeping the system alive* — identical in spirit to theboringoffice's office floor.
- **Motion vocab:** **pixel sprites with idle bounce** — the only living elements in the whole video. All else is static UI.

### ACT 2 — numbers (frame f005)
- **Content:** analytics card: purple bars (Proposal/Negotiation/Won) + donut "Open Pipeline by Stage" `$ value` — Discovery $102K teal, Qualification $65K violet, Proposal $28K yellow, Negotiation $120K orange.
- **Meaning:** apps acquire native analytic skin — dashboards become readable at a glance.
- **Motion vocab:** chart slide-up + count-in transitions; figures resolve fast (no lofi placeholder).

### ACT 2.5 — "Connect" (frame f006)
- **Content:** typing **"Con▌"** alongside **floating widget thumbnails** pinned at every canvas corner; a high-fidelity code/tree doc panel on the right.
- **Meaning:** apps hook into the box's system (debuggers, properties, APIs — the connective tissue).
- **Motion vocab:** thumbnails scatter into frame from edge-of-canvas with tiny delay stagger; heavy panel vaults (right side).

### ACT 3 — agents as dots (frame f007, key frame ⭐)
- **Content:** near-white field filled edge-to-edge with a **grid of white dots**, plus ONE **violet dot cluster** and sparse dark outliers.
- **Meaning:** the system's *bird's-eye view*: agents = points of an architecture. Every dot is a worker assigned to some task; the violet group is the selected department at work.
- **Motion vocab:** dot-field zoom-out reveal; cluster glow/pulse highlight.

### ACT 3.5 — ask-hold (frame f008)
- **Content:** brown card again: **"Ask anything. Your files an▌"**
- **Meaning:** after the heavy demo pages, invites natural-language recall back to YOU — "your files and your spirit, sir? ask."
- **Motion vocab:** typewriter with delay; sentence consciously truncated as a cliffhanger (fully revealed on the last card).

### ACT 4 — "Dream outcome" breathes (frame f009)
- **Content:** **Marketing Workspace app** — sidebar (`-app`, `-hr`, `marketing`, `brainlytics`), main page with form fields (project name, URL, brand tagline), a preview sidebar, and an accent-envy button.
- **Meaning:** business office as an app: marketing campaigns reified into a dashboard you create *by sentence*. The prompt-space from ACT 0 has compounded: you no longer prompt for an app — you keep prompting for iterations.
- **Motion vocab:** page swap cut; new screenshot with calm visibility.

### ACT 4.5 — widgets multiply (frame f010)
- **Content:** **"Interactive Images · Dogs Net"** — a masonry photo gallery of puppies with overlay heart icons.
- **Meaning:** see the span: CRM pipeline, marketing dashboard, and a fun consumer widget from the same instruction space. Range argument.
- **Motion vocab:** image-grid cascade.

### ACT 5 — agents keep it running (frame f011, final key frame ⭐)
- **Content:** dark scene with **concentric dashed orbital rings**. Pixel-dot agents **orbit** around centrally-embedded image blobs (gallery screenshots pinned at some rings' centers). Purple accent strip at top, footer-phase rail ("Dream outcome | Solution | Demo | Outro") persists at the bottom.
- **Meaning:** the closing formula: **agents don't wait — they circle, maintain, orbit. The system keeps running while you sleep.** This is the exact visual thesis of theboringoffice's ambient floor (walkers, tea-machine steam, whiteboard meetings).
- **Motion vocab:** slow-ring rotation with sprites anchored to orbit ellipses; images previewed at ring centers (the "watching over their work" frame).

### ACT 6 (implied end-card beyond f011)
- The audio waveform at the bottom proves: synced narration **voiceover** covers the whole 68.6s — "with one prompt" … "agents keep it running" … "Ask anything." — matching the four act rails.

---

## Global design system observed
| Element | Spec |
|---|---|
| Palette | charcoal `#141211` darks, warm olive/brown `#453d33` cards, paper white, violet accent `#8b5cf6`, teal `#14b8a6`, yellow `#eab308`, soft neutrals `#e7e5e4` |
| Type | clean geometric sans, semibold, ~40px on 1080×778 |
| Furniture | footer strip with 4 act labels persistent; bottom white audio waveform (voiceover) |
| Motion vocabulary | static dawn-cards + typewriter & block cursor; hard pop-in UI screenshots; **pixel sprites bouncing in place**; dot-grid zoom-out reveal; slow orbital dashed rings; wave scrub synced |
| Editing | hard cuts on ~6s beats; each new channel (UI → charts → connect → dots → apps) gets one static establishing card and nothing after |

---

# The same structure mapped for theboringoffice (Remotion)

| Act beat | Video's version | Ours |
|---|---|---|
| **0 · hook** | "with one prompt." dark card | "with one prompt. build an app. **in your terminal.**" — noir dark card, typewriter |
| **1 · solution** | Deal Tracker UI | office floor UI floor (boxed cards of boss desk, threads, work board) as **mock UI cards** (CSS-built, no screenshots needed) |
| **2 · numbers** | bar+donut cards | token&diff counters, cache-fresh data cards |
| **3 · demo/agents ⭐** | pixel sprites + dot-grid + orbital rings | pixel-office sprites (tekton/skopos/dikastes/mnemosyne/CTO glyphs) on an ASCII-scaffold canvas, colored dots for the agent roster, orbital rings circling the office |
| **4 · outro** | "Ask anything. Your files and…" | "**Ask anything. The office is working.**" — end-card tagline on noir, waveform footer |

## Remotion structure (per best-practices in the skill's rules for sequencing, text-animations, and timing)
- `video/` package: `remotion.config.ts`, `package.json`, `src/index.ts` (`registerRoot`), `src/Video.tsx` (4 `Sequence` acts, 30fps, 1080×778).
- Components: `TypewriterCard.tsx`, `UIPipelineMock.tsx` (CSS-structured cards — pure JSX boxes, no images), `PhotoGridMock.tsx`, `PixelSprite.tsx` (idle bounce via `spring` + keyframes from the skill's rules), `DotsConstellation.tsx`, `OrbitalRings.tsx` (SVG circles + sprite dots rotated), `FooterStrip.tsx`, `WaveformStrip.tsx`.
- Palette + Inter font from Google Fonts (skill: `loadFont` of `@remotion/google-fonts/Inter`).
- Typewriter by frame-driven substring (skill's `text-animations.md` pattern); pop-ins via `Series.Sequence` + `spring`.
